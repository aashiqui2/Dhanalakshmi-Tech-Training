#include <stdio.h>
int main()
{
    // FILE *fptr;
    // fptr = fopen("text.txt", "r");
    // printf("%c",fgetc(fptr));
    // printf("%c",fgetc(fptr));
    // printf("%c",fgetc(fptr));
    // printf("%c",fgetc(fptr));

    // char c;
    // while((c=fgetc(fptr))!=EOF)
    // {
    //     printf("%c",c);
    // }
    // char c[100];
    // while(fgets(c,sizeof(c),fptr)!=NULL)
    // {
    //      printf("%s",c);
    // }

    // int a, c;
    // float b;
    // while (fscanf(fptr, "%d %f %d", &a, &b, &c) == 3)
    // {
    //     printf("%d %f %d", a, b, c);
    // }

    FILE *fptr;
    fptr=fopen("abc.txt","w");
    // fputc('a',fptr);
    // fputc('a',fptr);
    // fputc('a',fptr);
    // fputc('a',fptr);
    char c;
    // while((c=getchar())!='\n')
    // {
    //     fputc(c,fptr);
    // }

    //  fputs("hi hello wolrd",fptr);





    return 0;
}